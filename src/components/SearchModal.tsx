import { tagItem } from "@/pages/SignUpPage";
import styled from "styled-components";

const SearchModal = ({ options }: tagItem[]) => {
  return <ModalStyled>asdf</ModalStyled>;
};

export default SearchModal;

const ModalStyled = styled.div`
  width: 300px;
  height: 300px;
`;
