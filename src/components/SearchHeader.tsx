import styled from "styled-components";

const SearchHeader = () => {
  return (
    <Wrapper>
      <TagListStyled></TagListStyled>
    </Wrapper>
  );
};

export default SearchHeader;

const Wrapper = styled.div``;

const TagListStyled = styled.div``;
